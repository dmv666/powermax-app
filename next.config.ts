import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['res.cloudinary.com','picsum.photos','ui-avatars.com', 'lh3.googleusercontent.com'],

  },
};

export default nextConfig;
